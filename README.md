# cub3D - Raycasting Game Engine

![Language](https://img.shields.io/badge/Language-C-blue) ![Library](https://img.shields.io/badge/Library-MiniLibX-orange) ![Score](https://img.shields.io/badge/Score-100%2F100-success)

**cub3D**는 고전 FPS 게임 *Wolfenstein 3D*에서 영감을 받아 제작된 간단한 3D 게임 엔진입니다. C 언어와 MiniLibX 라이브러리를 사용하여 구현되었으며, **레이캐스팅(Raycasting)** 알고리즘을 통해 2D 맵 데이터를 3D 시점으로 렌더링합니다.

## 📸 Preview
*(여기에 게임 실행 스크린샷이나 gif를 추가하면 좋습니다)*

## ✨ 주요 기능 (Features)
* **Raycasting 렌더링:** DDA(Digital Differential Analyzer) 알고리즘을 사용한 효율적인 벽 렌더링.
* **텍스처 매핑:** 동(East), 서(West), 남(South), 북(North) 방향에 따른 서로 다른 벽 텍스처 적용.
* **플레이어 이동:**
    * `W`, `A`, `S`, `D` 키를 이용한 이동.
    * `←`, `→` 화살표 키를 이용한 시점 회전.
    * 벽 충돌 처리 (Sliding Movement 적용).
* **맵 파싱:** `.cub` 설정 파일을 읽어 텍스처 경로, 바닥/천장 색상, 맵 구조를 파싱.
* **에러 처리:** 잘못된 맵 파일이나 설정에 대한 견고한 에러 핸들링 및 메모리 누수 방지.

## 🛠️ 설치 및 실행 (Installation & Usage)

이 프로젝트는 Linux 환경(MinilibX Linux 버전)에서 실행되도록 설정되어 있습니다.

### 1. 요구 사항 (Requirements)
* `gcc` 또는 `clang`
* `make`
* X11 및 MLX 관련 라이브러리

```bash
# Ubuntu/Debian 기준 필수 라이브러리 설치
sudo apt-get update
sudo apt-get install gcc make xorg libxext-dev libbsd-dev
```

### 2. 빌드 (Build)
레포지토리를 클론하고 `make`를 실행합니다.

```bash
git clone https://github.com/username/cub3d.git
cd cub3d
make
```

### 3. 실행 (Run)
실행 파일과 함께 `.cub` 맵 파일 경로를 인자로 전달합니다.

```bash
./cub3D maps/valid_classic.cub
```

## 🎮 조작법 (Controls)

| Key | Action |
| :---: | :--- |
| **W** | 앞으로 이동 |
| **S** | 뒤로 이동 |
| **A** | 왼쪽으로 이동 (Strafe) |
| **D** | 오른쪽으로 이동 (Strafe) |
| **←** | 왼쪽으로 시점 회전 |
| **→** | 오른쪽으로 시점 회전 |
| **ESC** | 게임 종료 |
| **X 버튼** | 게임 종료 (윈도우 상단) |

## 🗺️ 맵 설정 파일 (.cub)

`maps/` 폴더 내의 `.cub` 파일은 다음 규칙을 따릅니다.

1.  **텍스처 경로:** 동서남북 벽에 사용할 XPM 파일 경로.
2.  **색상 설정:** 바닥(F)과 천장(C)의 RGB 색상 (0~255).
3.  **맵 레이아웃:**
    * `1`: 벽
    * `0`: 빈 공간
    * `N`, `S`, `E`, `W`: 플레이어 시작 위치 및 시선 방향

**예시 (`valid_classic.cub`):**
```text
NO ./textures/north.xpm
SO ./textures/south.xpm
WE ./textures/west.xpm
EA ./textures/east.xpm

F 220,100,0
C 225,30,0

        1111111111111111111111111
        1000000000110000000000001
        1011000001110000000000001
        1001000000000000000000001
111111111011000001110000000000001
100000000011000001110111111111111
11110111111111011100000010001
11110111111111011101010010001
11000000110101011100000010001
10000000000000001100000010001
10000000000000001101010010001
11000001110101011111011110N0111
11110111 1110101 101111010001
11111111 1111111 111111111111
```

## 📂 파일 구조 (File Structure)

```
.
├── includes/          # 헤더 파일 (cub3d.h, get_next_line.h 등)
├── libft/             # Libft 라이브러리
├── minilibx-linux/    # MiniLibX 라이브러리
├── maps/              # 맵 파일 (.cub)
├── textures/          # 텍스처 이미지 (.xpm)
├── srcs/              # 소스 코드
│   ├── main.c         # 메인 진입점 및 초기화
│   ├── loop.c         # 메인 루프 및 훅 관리
│   ├── parsing.c      # 맵 파싱 로직
│   ├── raycasting.c   # 레이캐스팅 계산
│   ├── dda.c          # DDA 알고리즘 수행
│   ├── render.c       # 화면 렌더링 및 텍스처 매핑
│   ├── key_event.c    # 키 입력 처리 (이동, 회전)
│   └── ...
└── Makefile           # 빌드 스크립트
```

## 👨‍💻 Authors
* **juyoukim**
* **taewonki**
